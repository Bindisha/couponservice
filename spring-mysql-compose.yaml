version: '3.8'

services:
  mysql:
    image: mysql
    container_name: mysql-docker
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: servicedb
      MYSQL_USER: sachin
      MYSQL_PASSWORD: password
    ports:
      - "3307:3306"
    networks:
      - springboot-mysql-network
    volumes:
      - mysql_data:/var/lib/mysql  

  springboot-app:
 #   build: .
    image : sachingowd07/couponservice:v5
    container_name: springboot-container
    ports:
      - "9091:8080"
    environment:
      DB_HOST: mysql-docker
      DB_PORT: 3307
      DB_NAME: servicedb
      DB_USER: sachin
      DB_PASSWORD: password
    depends_on:
      - mysql
    networks:
      - springboot-mysql-network

networks:
  springboot-mysql-network:
     external: true

volumes:
  mysql_data: